/*#pragma once
#include "AirportGraph.h"
#include <vector>

#include "Airport.h"
class MST {
public:
  static std::vector<Flight *> primMST(const Graph &graph);
  static std::vector<Flight *> kruskalMST(const Graph &graph);
};*/
